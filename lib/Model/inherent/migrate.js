/**
 * #Model.prototype.migrate()
 *
 * Migrate this model's underlying physical collection to match
 * the current in-memory ontology.
 *
 * @param {Function} cb
 * @api public
 */

module.exports = function migrateModel (cb) {
  cb(new Error('Not implemented yet!'));
};
