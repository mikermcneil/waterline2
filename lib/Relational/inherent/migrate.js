/**
 * #Relational.prototype.migrate()
 *
 * Migrate this relation's underlying physical collection to match
 * the current in-memory ontology.
 *
 * @param {Function} cb
 *
 * @api public
 */

module.exports = function migrateRelation (cb) {
  cb(new Error('Not implemented yet!'));
};
